# Пример файла переменных окружения
# Скопируйте этот файл в .env и заполните нужные значения
# 
# ВАЖНО: .env файл НЕ включается в Docker образ для безопасности
# Переменные окружения передаются при запуске контейнера

# Путь для сохранения скачанных видео
# Если не указан, используется временная директория внутри контейнера
DOWNLOAD_PATH=/app/downloads

# Другие переменные окружения (если нужны в будущем)
# PORT=8000
# DEBUG=false
# LOG_LEVEL=INFO

